management:
  endpoints:
    web:
      exposure:
        include: refresh, health, info
teste: 
  fonte: "config-git, valor-dev."
feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: full
