management:
  endpoints:
    web:
      exposure:
        include: refresh, health, info
  health:
    circuitbreakers:
      enabled: true
teste: 
  fonte: "config-git, valor-dev."
feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: full
